<h3 class="bg-dark text-light text-center px-3 pt-3 pb-1">Entités</h3>
<ul class="nav flex-column">
{% for entityName in allEntities|sort %}
<li class="nav-item">
  <a class="nav-link" href="{{ path('admin_index') ~ (entityName|lower) }}">{{ entityName }}</a>
</li>
{% endfor %}
</ul>