{% extends 'base.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_admin_index') }}">back to list</a>

    <a href="{{ path('app_commentaire_admin_edit', {'id': commentaire.id}) }}">edit</a>

    {{ include('commentaire_admin/_delete_form.html.twig') }}
{% endblock %}
