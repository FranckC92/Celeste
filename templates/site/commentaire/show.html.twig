{% extends 'base.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ commentaire.Commentaire }}</td>
            </tr>
            <tr>
                <th>CommentaireID</th>
                <td>{{ commentaire.commentaireID }}</td>
            </tr>
            <tr>
                <th>OnetoMany</th>
                <td>{{ commentaire.OnetoMany }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ commentaire.slug }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ commentaire.auteur }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ commentaire.contenu }}</td>
            </tr>
            <tr>
                <th>DateCreation</th>
                <td>{{ commentaire.dateCreation ? commentaire.dateCreation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DateModification</th>
                <td>{{ commentaire.dateModification ? commentaire.dateModification|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_site_commentaire_index') }}">back to list</a>

    <a href="{{ path('app_site_commentaire_edit', {'id': commentaire.id}) }}">edit</a>

    {{ include('site/commentaire/_delete_form.html.twig') }}
{% endblock %}
